# 個人開発のGitHub運用ルール（必ず守ること）

このファイルは、個人開発（趣味の開発）の共通ルールをまとめたものです。
案件ごとに書き換える必要はなく、そのまま使い回します。
（このファイルは、同じフォルダの CLAUDE.md から「@github-rules.md」の記述で読み込まれます）

このフォルダの成果物は、私個人のGitHubアカウントのPrivateリポジトリで管理します。
Ownerとするアカウント名は repo-settings.md の「GitHubアカウント」の行に記載します。
会社・所属組織のOrganizationのリポジトリは、このルールの対象外であり、使用しません。

## 作業を始めるとき（毎回）

作業を始めるとき、Claudeはまず次の順番で確認・準備をしてください。

1. このフォルダがGitHubのリポジトリと結びついているか確認する
2. まだ結びついていない場合（新しいツールをこれから作る場合）は、
   最初に私の個人アカウントへ新しいPrivateリポジトリを作成する。次の条件を必ず守ること。
   - Ownerは私の個人アカウント（repo-settings.mdに記載。未記載なら私に確認して記載する）
   - 公開範囲はPrivate（Publicにしない。公開したくなった場合は別途私が指示する）
   - 内容が分かるリポジトリ名にする（例：attendance-report-tool、sales-data-automation）
     test、sample、new、project1、aaa のような分かりにくい名前は避ける
     （物になるか分からない試作段階なら、名前の末尾に -test を付けてもよい）
   - 用途をDescriptionとREADMEに記載する
   - 適切な.gitignoreを設定する（docsフォルダを除外する設定を含める）
   - 作成前に設定内容を表示して私の確認を取る
   - 会社・所属組織のOrganizationには作成しない
   リポジトリ作成後、初期状態をCommit・Pushしてから作業に入る
3. すでに結びついている場合は、次の開始手順を行う
   - 現在のフォルダが、repo-settings.mdに記載の私の個人アカウントのリポジトリであることを確認する
   - マージされていないPull Request（Open状態）が残っていないか確認し、
     残っていれば内容を報告して指示を待つ（マージ未了のまま新しい作業に入らない）
   - 未保存の変更がないか確認する
   - 問題がなければGitHubから最新の変更をPullする
   - releaseフォルダ内にメモファイル（「〇〇はReleasesにあります」「〇〇はUPしていません」等）があれば、
     該当する完成品の実物がこのPCの手元にないことを、開始時の報告に含めて私に伝える
4. どちらの場合も、mainブランチを直接変更せず、今回の作業用ブランチを作成してから作業に入る
5. 未保存の変更や競合の可能性がある場合は、何も変更せずに報告する

※「最初にリポジトリを作ってから作業する」がこの運用の原則です。
　手元だけで作り始めてしまうと、PCの故障で失われるうえ、後からまとめて保存したときに
　大量のCommitが一度に並んで履歴が読みにくくなるため、必ず箱（リポジトリ）が先です。

## mainの上では変更しない（常時の原則）

- 現在のブランチがmainのときは、ファイルの作成・変更・削除を行わない。
  これはセッションの最初だけでなく、会話の途中・マージ完了後の追加依頼でも常に適用する
- マージが完了した時点で1つの作業サイクルは終了。続けて新しい依頼
  （追加修正、調査で見つかった問題の修正、微調整、「ついでに〇〇」を含む）を受けたときは、
  変更に着手する前に、内容に合った名前の作業用ブランチを新しく作成してから始める
- 変更を伴わない調査・確認・閲覧だけであれば、mainのままで行ってよい。
  ただし、その調査の流れでファイルを作る・変更する必要が生じた時点で、
  先に作業用ブランチを作成してから行う
- もしmainの上で変更してしまったことに気づいた場合は、直ちに報告し、
  変更内容は消さずに新しい作業用ブランチへ移して、mainを変更前の状態に戻す

## 完成品の保存とバージョン管理（release フォルダ）

- 完成品（配布する実行ファイル・HTMLツール等）は、必ずこのフォルダ直下の release フォルダに保存する。
  案件ごとに保存先の名前を変えず、常に release で固定する
- 完成品は必ずバージョン管理する。命名は「ツール名_v1.01_260830.exe」の形式。
  ・初回完成時は v1.01 から始める
  ・更新作業を行うごとに 0.01 ずつ増やす（v1.01 → v1.02 → v1.03 …）
  ・末尾に完成日（更新日）を6桁で付ける（2026年8月30日 → 260830）
- 過去バージョンのファイルは release 内に残す（整理が必要なほど増えた場合は私に相談する）
- バージョンを更新したら、WORKLOG.mdに「バージョン・日付・変更内容」を記録する
- release 内の完成品は、exe等の実行ファイルを含めてGitHubへの保存対象とする。
  保存先は通常の成果物と同じ、私の個人アカウントのPrivateリポジトリ内（このフォルダのrelease）であり、
  別の場所には保存しない
- ただしGitHubはリポジトリ内1ファイル100MBが上限（50MB超で警告）のため、
  完成品が100MBを超えた場合は、勝手に判断せず、UPする前に必ず私にどうするかを確認する。
  選択肢は次の2つで、それぞれの意味を添えて提示する。
  ・GitHub Releases（同じリポジトリに付属する配布物置き場機能。1ファイル2GBまで）にUPする
  ・GitHubにはUPしない（このPCのreleaseフォルダにだけ置く）
- 「UPしない」を選んだ場合は、releaseフォルダ内に
  「今回のツール名_v1.22_260830は100MBを超えるためGitHubリポジトリにUPしていません」のような
  メモファイル（英数字のファイル名、例：v1.22_not-uploaded.txt）を置き、
  WORKLOG.mdにも同じ内容を記録する。
  ※この場合、完成品の実物は別のPCには引き継がれない（設計図から再ビルドは可能）ことも、
  　選択時に私へ伝えること
  ReleasesもPrivateリポジトリの一部であり、閲覧・ダウンロードできるのは
  そのリポジトリへのアクセス権を持つ人（＝私）だけで、一般には公開されない。
  Releasesを使う場合も、私の個人リポジトリ以外の場所・外部サービスへのアップロードは行わない
- 注意：Releasesに置いたファイルは、Gitの履歴データとは別の棚のためClone・Pullでは自動で降りてこない。
  別のPCでそのファイルが必要になった場合は、Claudeが gh release download で同じPrivateリポジトリの
  Releasesから取得する（私が「Releasesの完成品をダウンロードして」と頼めばよい）。
  Releasesへ完成品を置いたときは、WORKLOG.mdに「どのバージョンをReleasesに置いたか」を記録し、
  releaseフォルダ内にも同名のメモファイル（例：ツール名_v1.03_260830_はReleasesにあります.txt）を置いて、
  Cloneした先でも存在に気づけるようにする

## 使用言語と配布形態の選択

- 使用するプログラミング言語はPythonに固定しない。作る内容・完成品の容量・配布のしやすさを考慮して、最適な選択肢を選ぶ
- 目安：
  ・ドラッグ&ドロップ等のシンプルなワンアクションツール → Python（exe化）または HTML+JavaScript
  ・ブラウザで完結できるもの → HTML+JavaScript（1ファイルで容量が小さく、インストール不要で配布が最も簡単）
  ・機能が多い本格的なアプリ → 内容に応じて選定し、インストーラ形式も選択肢に含める
- 開発を始める前に、選んだ言語・配布形態（単体exe／HTMLファイル／インストーラ形式）について、
  Claudeの推奨を1つ明確に示し、次を必ず添えて私の同意を得る
  ・その選択のデメリット
  ・完成品のおおよその容量
  ・後から言語や形態を変更する場合に、やり直しがどの程度発生するか
- 私が同意した内容をWORKLOG.mdに記録してから開発に入る

## docs-shared（個人情報を除去した資料の共有）

docs フォルダの原本はこれまでどおりGitHubに保存しない。ただし、別のPCとの併用作業のために、
個人情報を除去した資料のコピーは docs-shared フォルダに置いてGitHubへ保存してよい。

個人情報の検出・除去はClaudeが責任を持って徹底的に行う（私が資料を目視で全確認することは前提にしない）。

- 手順（コマンド /sanitize-docs でも実行できる）
  1. 対象資料のコピーを作り、個人情報を伏せ字（[氏名] [住所] [電話番号] 等）に置換する。
     資料としての構成・数値・手順・ノウハウ部分は変更しない
  2. 除去の対象：氏名、住所、電話番号、メールアドレス、口座情報、
     会員番号・アカウント名、その他個人を識別できる情報
  3. 検出は必ず二段階で行う。
     【一次】パターン検出（電話番号・メール・住所などの形式的な検出）と文脈の読解の両方で走査する
     【二次】置換後のファイル全文をあらためて最初から読み直し、
     　　　　一次で漏れた個人情報が残っていないかを、別の視点（人名らしい固有名詞、
     　　　　文脈から個人や取引先が特定できる記述、画像・表・ヘッダー・フッター内の記載）で再点検する
  4. 迷ったら安全側に倒す。個人情報かどうか判断がつかない記述は除去する。
     除去すると資料の価値が大きく損なわれる場合は、そのファイル自体を共有対象から外し、その旨を報告する
  5. 二次点検までを終えたコピーを docs-shared に保存する（docs の原本は変更しない）
  6. 「ファイルごとの除去件数・除去した情報の種類・迷って除去した項目・対象から外したファイル」の
     要約レポートを私に見せる。私はこのレポートに対してOKを出すだけでよく、資料本文の目視は不要
  7. 私のOKの後にのみ、通常の保存手順でCommit・Pushする。OK前に docs-shared をPushしてはいけない
- 少しでも「漏れの疑いを消しきれない」と感じたファイルは docs-shared に置かず、理由を報告する
- GitHubに保存してはいけない情報（下記の章）に該当するものは、伏せ字化しても残る危険があれば
  docs-shared に置かない

## 作業の途中
- キリの良いところごとに、分かりやすい日本語のメッセージでCommitする（Pushは最後にまとめる）
- 作業が翌日以降に続く場合は、その日の終わりに必ず「保存して」の手順まで行う
  （PCの中に未Pushの作業を残したまま日をまたがない）

## 「保存して」と言われたとき／作業を終えるとき（毎回）
CommitだけでなくPush、Pull Request、マージ運用まで、次の順番で行う。

0. 例外的に、このフォルダがまだGitHubのリポジトリと結びついていなかった場合
   （開始時の作成を経ずに作られたフォルダの救済）は、ここで先に
   「作業を始めるとき」の2と同じ条件で新しいPrivateリポジトリを作成する。
1. 変更したファイルを一覧表示する
2. 関係のないファイルが含まれていないか確認する
3. パスワード、APIキー、個人情報、機密情報がないか確認する
4. テストを実行する
5. git remote -v でPush先を確認し、URLが「https://github.com/私の個人アカウント名/」（repo-settings.mdに記載のアカウント）で始まることを確認する。会社・他組織のOrganizationのURLだった場合や、記載と異なる場合はPushせず停止する
6. 分かりやすい日本語のメッセージでCommitする
7. 作業用ブランチをPushする（mainブランチを直接変更せず、作業用ブランチを作成してからPushする）
8. 内容が分かる日本語のタイトルと、変更内容の要約を本文に記載したPull Requestを作成する
9. Pull Requestの変更内容を要約して私に見せる際、
   あわせてマージ方式の推奨を理由付きで提案し、私の選択を確認する。
   ・通常マージ（Commitをそのまま全部mainに残す）
     → 各Commitが独立した意味を持つ回（別々の修正や機能を積んだ等）に推奨
   ・Squashマージ（細かいCommitを1件のまとめCommitに圧縮）
     → 試行錯誤や細かい修正中心で、mainには結論だけ残せばよい回に推奨
   私が選択した方式でマージしてmainを更新し、役目を終えた作業ブランチを削除する。
   Squashを選んだ場合、まとめCommitのメッセージは「何をした作業か」が分かる日本語にする。
   また、Squash後の手元ブランチ削除は安全版（-d）が拒否される仕様のため、
   mainとの差分がゼロであることを確認したうえで強制版（-D）を使ってよい。
   mainが最新になったことを確認する
10. リポジトリおよびPull RequestのURLを報告する
問題が見つかった場合は、CommitやPushをせずに停止して報告する。

## マージについての原則
- mainが更新されるのはマージの瞬間。マージまで到達して1サイクル完了
- マージ未了のPull Requestを溜めない（保留状態を作らない）
- マージ方式（通常マージ／Squashマージ）は、マージのたびに選択する。
  Claudeは毎回、今回の作業内容に合った方式を理由付きで推奨し、私が選ぶ。
  迷った場合の目安：下書き的なCommitが多ければSquash、各Commitに独立した意味があれば通常マージ

## 外部サービスの使用禁止（コスト安全・最重要）

個人開発でも、費用が発生する仕組み・課金に化ける仕組みは一切作らないことを必須とします。
- ツールに外部サービスを組み込むことは原則禁止。有料APIや従量課金サービスはもちろん、
  無料枠のあるサービスも使用しない（長く使ううちに無料枠を消費・超過して課金に変わる
  仕組みが多いため、「無料枠がある」は「使ってよい」を意味しない）
- APIキーの取得、アカウント登録、クレジットカード登録を前提とする仕組みは、
  無料をうたうものであってもすべて使用しない
- 処理はPCの中だけで完結する方法（ローカル処理）を基本とし、まずローカルでの実現を検討する。
  ローカル処理で実現できない要望だった場合は、代替手段を勝手に実装するのではなく、
  「ローカル処理では実現できません」と説明したうえで、下記の条件を満たす
  無料の汎用サービスで実現可能ならそれを選択肢として提案し、私の判断を仰ぐ
- 次の条件をすべて満たす汎用のネットサービス（例：汎用の地図の表示）に限り、使用の選択肢にしてよい。
  ・完全に無料で、利用枠（無料枠・回数制限）の消費という概念がない
  ・アカウント登録、APIキー、クレジットカード登録が不要
  ・誰のPCでも追加の契約なしにそのまま動く
  ただしこの場合も、Claudeの判断だけで組み込まず、実装前に
  「目的・使用するサービス名・どんな通信が発生するか」を説明し、私の確認を得てから組み込む
- 上記の確認を得て外部通信を使う場合でも、機密情報・個人情報・業務データそのものを
  外部へ送信する作りにはしない（外部から取得して表示する用途を基本とする）
- 例外を認めるかどうかの最終判断は私が行う。Claudeの判断で「無料だから」
  「便利だから」という理由で外部サービスや外部通信を組み込むことは、いかなる場合もしない

## GitHubに保存してはいけない情報（Privateでも禁止）
パスワード、APIキー、アクセストークン、秘密鍵、電子証明書、ログイン情報、
マイナンバー、自分や家族・知人の個人情報、他人の個人情報を含むデータ、
認証情報が記載された設定ファイル。
これらを見つけた場合は、中身を表示せず、ファイル名と問題の種類だけを報告する。

## リポジトリの削除について（個人用の扱い）
- 個人の遊び・実験用リポジトリなので、私が「このリポジトリを削除して」と指示すれば削除してよい
- ただし実行前に、削除対象のリポジトリ名とURLを表示して、対象に間違いがないか私の最終確認を必ず取る
　（似た名前のリポジトリの取り違えを防ぐため。確認なしの削除は行わない）
- Claudeの判断で削除を提案するのは構わないが、私の指示なしに実行はしない

## 私の明示的な指示なしに実行してはいけない操作
- ブランチの削除（マージ済みの作業ブランチの削除は、私の確認を取ればOK）
- PrivateからPublicへの変更（公開は私が明示的に指示した場合のみ）
- mainブランチへの直接Push、Force Push
- Gitの履歴の書き換え
- 大量のファイル削除
- 会社・業務のデータやコードを、この個人リポジトリへ保存すること
  （業務の成果物は会社の仕組みで管理する。個人と会社を混ぜない）
これらの操作が必要になった場合は、理由と影響を説明して停止する。

## 認証について
- パスワード、認証コード、アクセストークンをこのチャットに入力するよう求めない
- ログインが必要な場合は、ブラウザの認証画面で本人がログインするよう案内する

# 進め方のお願い

- 何かを実行したり変更したりする前に、「これから何をするか」を先に説明してください
- 一度にたくさん進めず、区切りごとに確認を取ってください
- 特に次の場面では、必ず作業を止めて私の同意を得てから進めてください（勝手にどんどん進めない）
  ・ライブラリやソフトウェアをインストールするとき
  ・PCの設定・環境・このフォルダ以外のファイルに変更を加えるとき
  ・開発の方向性に複数の選択肢があるとき、当初の想定から方針を変えるとき
- 例外：会話の中で私が「確認不要で進めて」等の直接指示をした場合は、その指示を優先して
  構いません（急ぎの場合など）。ただし、その次の作業からは上記の基本に戻ってください
- うまくいかないときは、原因と対処を私にもわかる言葉で説明してください
- 判断できない問題や、上記の禁止操作が必要になった場合は、作業を止めて私に相談してください

# やらないでほしいこと

- 指示されていないファイルを削除しないでください
- docsフォルダの中の資料を書き換えないでください（読むのはOKです）
- CLAUDE.md、PROJECT.md、github-rules.md、repo-settings.md、.claudeフォルダの中身を、指示なく書き換えないでください
  （repo-settings.mdの「GitHubアカウント」の行だけは、初回設定時に私へ確認のうえClaudeが記入してOKです。
  　PROJECT.mdは私が「やりたいこと」を書く専用の場所です）

# 作業ツールの切り替え（Desktopアプリ／VS Code／ターミナル）

私はClaude CodeをDesktopアプリで使う日もVS Codeで使う日もある。どちらで開いても同じように作業できること。

- DesktopアプリのCode機能・VS Code拡張・ターミナルのclaudeコマンドは、すべて同じClaude Codeであり、
  同じこのフォルダを開けば、CLAUDE.md・各ルール・.claudeの安全装置・ファイルとGitの状態はすべて共通に効く。
  ツールを切り替えるための特別な操作は何も必要ない
- ただし会話の履歴（セッション）はツール間で引き継がれない。
  そのため、作業の続きは会話の記憶に頼らず、WORKLOG.mdとGit・GitHubの状態だけから再開できる状態を常に保つこと
- 作業を中断・終了するときは、WORKLOG.mdに「どこまで終わったか」と「次にやること」を必ず書いてから終える。
  これにより、次回どのツール・どのPCから開いても、開始手順のWORKLOG確認だけで続きに入れる
- 同じプロジェクトのフォルダを、複数のツールで同時に開いて作業しない（一度に使うのは1つ。
  同時使用は変更の衝突や履歴の不整合のもとになる）

# 作業の記録

- 作業のたびに、このフォルダ直下の WORKLOG.md（作業ログ）に「日付・何をしたか・Pull RequestのURL・マージ状況・完成品のバージョン更新・次にやること」を短く追記してください
- 前回までの経緯が必要なときは、まず WORKLOG.md を読んでください
- このログは成果物と一緒にGitHubへ保存し、別のPCでも経緯を引き継げるようにしてください
- ファイル名は、zip解凍時の文字化け防止のため英字（WORKLOG.md）で固定です。日本語名に変更しないでください。今後、このフォルダに新しく作るファイル・フォルダの名前も英数字を基本としてください（docs内の資料は日本語名で構いません）
