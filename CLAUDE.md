# このファイルについて

このCLAUDE.mdは設定の表紙です。案件ごとに書き換える必要はありません。
やりたいことや案件ごとの情報は、同じフォルダの PROJECT.md に書きます（人間が書くのはPROJECT.mdだけです）。

# 私について

- 趣味で個人開発をしています。プログラミングの知識はありません
- 説明はすべて日本語でお願いします
- 専門用語はできるだけ使わず、使う場合はひとこと意味を添えてください
- コードの中身の解説よりも、「これで何ができるようになるか」を教えてください

# 案件の内容（やりたいこと）

やりたいことは次のファイルに書いてあります。作業開始時に必ず読んでください。
このファイルは人間が「やりたいこと」だけを書く場所です。Claudeがルールや設定を書き足してはいけません。

@PROJECT.md

# 案件設定（GitHubアカウント）

このリポジトリのOwnerとなる私の個人アカウント名などの案件設定は次のファイルにあります。作業開始時に必ず読んでください（Claudeが管理するファイルです）。

@repo-settings.md

# 個人開発のGitHub運用ルール（必ず守ること）

個人開発の共通ルールは、同じフォルダにある github-rules.md にまとめてあります。
下の行で、そのルールをすべて読み込みます。この行は消さないでください。

@github-rules.md

# docsフォルダの扱い（重要）

参考資料は docs フォルダに入っています。作業を始める前に、まず docs フォルダの中身を必ず一覧して確認してください（一覧メモは廃止したので、フォルダを直接見るのが唯一の確認方法です）。
- docsフォルダは .gitignore で除外済みのため、GitHubには保存されません（このPCの中だけの資料置き場です）
- 個人情報を含む資料が入る前提なので、docsの中身をCommit・Pushの対象に含めないでください
- 保存前の確認（変更ファイルの一覧表示）の際、docsの中身が含まれていないことも確認してください
- 新しくリポジトリを作成する場合（github-rules.mdの手順）も、.gitignoreにdocsフォルダの除外設定を必ず含めてください
- docs内の資料を別のPCと共有したい・GitHubへ入れたい場合は、github-rules.mdの「docs-shared（個人情報を除去した資料の共有）」の手順に従ってください（個人情報の検出・除去はClaudeが二段階チェックで確実に行い、原本は動かさず伏せ字コピーをdocs-sharedに置きます。私は除去結果の要約レポートにOKを出すだけでよく、資料本文の目視確認は不要です。レポートへのOK前にPushしてはいけません）
